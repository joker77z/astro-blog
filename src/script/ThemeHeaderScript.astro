<!-- is:inline을 추가해서 module형식이 아니라 html에 포함되게 한다. -->
<script>
  const theme = localStorage.getItem("theme");

  if (
    theme === "dark" ||
    (!theme && window.matchMedia("(prefers-color-scheme: dark)").matches)
  ) {
    document.body.classList.add("dark");
  }
</script>
